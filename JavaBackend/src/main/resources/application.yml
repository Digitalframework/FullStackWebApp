

server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: digitalframeworksm@gmail.com
    password: spygvjmvmsugsgyp
    smtp.auth: true
    starttls.enable: true
  datasource:
    password: 2708
    url: jdbc:postgresql://localhost:5432/jwt_security
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true